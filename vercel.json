{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm --prefix web ci",
  "buildCommand": "npm --prefix web run build && node web/scripts/vercel-routes.mjs web/.vercel/output/config.json && rm -rf .vercel/output && mkdir -p .vercel && mv web/.vercel/output .vercel/output",
  "outputDirectory": ".vercel/output"
}
